## TaleSpinner Exp - example environment (repo root)
# Copy to `.env` (in THIS folder) and adjust values.
#
# IMPORTANT:
# - OpenRouter token must live only here (env) and should never be stored in DB / returned by API.

# ---------------------------
# General settings
# ---------------------------
PROJECT_NAME=TaleSpinner API
VERSION=0.1.0
DEBUG=True

# ---------------------------
# API settings
# ---------------------------
API_V1_PREFIX=/api/v1

# ---------------------------
# CORS settings (comma-separated)
# ---------------------------
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ---------------------------
# Database
# ---------------------------
DATABASE_URL=sqlite:///./talespinner.db

# Required if you use `/api/v1/tokens` endpoints (token encryption at rest)
# Generate with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# ---------------------------
# Providers (base URLs)
# ---------------------------
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OLLAMA_BASE_URL=http://localhost:11434

# ---------------------------
# Current mode: models via env
# ---------------------------
MODELS_FROM_ENV_ONLY=true

# OpenRouter (LLM)
OPENROUTER_API_KEY=
OPENROUTER_MODELS=anthropic/claude-3.5-sonnet,openai/gpt-4o-mini
# Optional (if empty -> first item from OPENROUTER_MODELS)
OPENROUTER_DEFAULT_MODEL=

# ---------------------------
# Preset: 4 LLM models (main/rag/guard/storytelling)
# ---------------------------
# If *_MODEL is empty -> falls back to OPENROUTER_DEFAULT_MODEL / first OPENROUTER_MODELS.
OPENROUTER_MAIN_MODEL=

RAG_ENABLED=false
OPENROUTER_RAG_MODEL=

GUARD_ENABLED=false
OPENROUTER_GUARD_MODEL=

STORYTELLING_ENABLED=false
OPENROUTER_STORYTELLING_MODEL=

# Ollama (Embedding)
OLLAMA_EMBEDDING_MODEL=nomic-embed-text
# Optional
OLLAMA_EMBEDDING_DIMENSIONS=


